/** @format */

const etat_enum = {
	values: ['a', 'r', 'ps', 'pr'],
	message: 'Etat incorrect',
}

const seance_enum = {
	values: ['matin', 'apres-midi'],
	message: 'Seance incorrect',
}

module.exports = {
	etat_enum,
	seance_enum,
}
